import styled from 'styled-components';

const Copy = styled.p`
  font-size: 1.6rem;
  font-weight: 400;

  color: var(--primary);

  letter-spacing: 0.05rem;
  line-height: 1.8rem;
`;

export default Copy;
